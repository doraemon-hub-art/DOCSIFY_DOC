> Shell 命令 & 脚本相关

---

> 条件测试操作符

- -e 判断指定为文件或者目录是否存;在
- -gt 判断 a -gt b, a 是否大于 b;
- -lt 判断 a -lt b, a 是否小于 b;

---

> *$

传递过来的所有参数；

> 等于号两边不能有空格

Shell变量赋值是紧凑的。

```bash
test="11"
```

---

> 脚本传参，指定参数存在还是参数数量混淆

```bash
# 是否够N个参数
if [ $# -eq N ]; then
    # 拿第 N 个参数
else
    # 默认值
fi

# 是否存在第N个参数
if [ -n "$N" ]; then

else 

fi
```

---

# zip

zip 压缩包命令相关。

> 参数

- -r 递归；
- -y 