> 有关数据埋点的思考

例如:

TODO:

- 数据埋点；
  - 记录数据的意义；
  - 关键数据分析问题；
  - 记录哪些数据；
    - 数据时间的关联，组合分析问题；
    - 数据时间的重叠，不要重复记录字段；
      - 精简&有用；
  - 记录方式&周期；
  - 在这其中的一些思考；
  - 对于链路耗时分析，收集时间点，而不是直接计算；
    - 计算操作交由分析工具；
  - 数据分析;
    - 单个 & 聚合；
  - 极端问题：
    - 建链 & 区分不同实例请求时间；
  - 后续扩展；
    - 添加版本号，如果更新结构体，以此区分，用于可视化分析的不同解析&渲染；
  - ......